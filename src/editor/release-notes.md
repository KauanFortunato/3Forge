# What's New

- When you update the app and reload the website for the first time, the editor shows this summary of what has changed.

- After closing the notification, the same version does not appear again in this browser.

- This content comes from `src/editor/release-notes.md`, so the release note can be edited without modifying the React component.